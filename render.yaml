services:
  - type: web
    name: ghost-atlas-map-of-reality
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm ci && npm test && npm run verify
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
