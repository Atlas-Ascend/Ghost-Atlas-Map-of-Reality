# Ghost Atlas Map of Reality

Executable reference implementation of the Ghost Atlas **Map of Reality** and Vishvarupa universal technical organism.

This repository is both architecture and running software. It exposes a canonical graph spanning cosmological lifecycle, ontology, Universal Atlas, SESHAT, Thoth, Atlas of Consciousness, NHCM, the original 23 Vishvarupa organs, command/authority, Packet OS, Workforce Spine, bounded execution, EDEN/ARK/Merkaba, Hypernet, proof/homeostasis, Ghost Atlantia, GAIA, Euna, manifestation and reality return.

## Truth boundary

The current software can prove its own model invariants, API and visualization implementation. It **does not** claim physical EDEN state, unattended resident autonomy, production golden-baseline sealing, or external commercial proof without receipts from those domains.

## Run

```bash
npm ci
npm test
npm run verify
npm start
```

Open `http://localhost:3000`.

## API

- `GET /health`
- `GET /api/v1/map`
- `GET /api/v1/layers`
- `GET /api/v1/nodes`
- `GET /api/v1/edges`
- `GET /api/v1/organs`
- `GET /api/v1/wheel`
- `GET /api/v1/campaign`
- `GET /api/v1/proof`
- `GET /api/v1/openapi.json`

## Laws

`Patch → Route → Verify → Promote`

`Generation Is Not Permission`

`Physical Truth Outranks Repository Truth`

`No False Green`
